{
  "schema_version": "1.0",
  "description": "openaiops-platform feature ledger. Cross-session contract; status transitions are real product gates.",
  "updated_at": "2026-05-28",
  "owner": "@huangbaixun",
  "source": "Pivoted from archived OpenAIOps repo per its ADR-0007 (2026-05-24).",
  "history": [
    {
      "date": "2026-05-24",
      "event": "Repo created + features.json with SLICE-0 (status: building)"
    },
    {
      "date": "2026-05-24",
      "event": "SLICE-0 done — CI green, 8/8 ACs verified (AC-7 partial: goose down not auto-tested)"
    },
    {
      "date": "2026-05-24",
      "event": "PRE-1 resolved — ADR-0002 chose ch-migrate service (Option B); scaffold landed + smoke verified"
    },
    {
      "date": "2026-05-24",
      "event": "PRE-2 resolved — ADR-0003 chose split cmd/query/ (Option B) under Heavy-soon workload assumption; spec + ADR landed, no code changes yet"
    },
    {
      "date": "2026-05-24",
      "event": "PRE-3 resolved — chquery package (MustTenantScope helper + Conn wrapper) + Row Policy template + prod CH custom_settings config + build-time lint. SLICE-1 unblocked. 7 commits 80344d7..ce7ed1b."
    },
    {
      "date": "2026-05-25",
      "event": "SLICE-1 spec landed (docs/specs/2026-05-25-slice-1-trace-design.md) — brainstormed Bearer-on-receiver tenant trust + ingester-as-OTLP-receiver (Collector out of data path) + chquery.Batch wrapper closing SLICE-1-PREP-1. AC #1/#3/#7/#9 evolved (see acceptance_criteria below). Next: writing-plans for docs/plans/2026-05-25-slice-1-plan.md."
    },
    {
      "date": "2026-05-26",
      "event": "SLICE-1 done — 16 tasks, ~27 commits, 10/10 ACs verified. Three bugs caught + fixed during execution (ingester IncludeMetadata both transports; CH replay-safety; frontend nginx /api/v1/traces routing). See deploy/SLICE-1-evidence.txt."
    },
    {
      "date": "2026-05-26",
      "event": "SLICE-1-PREP-1 resolved — chquery.Conn.PrepareBatch + *chquery.Batch wrapper landed in commit 1a00986; tenant pinned at PrepareBatch time, smoke-verified with TestSmoke_BatchTenantIsolation."
    },
    {
      "date": "2026-05-26",
      "event": "SLICE-2 spec landed (docs/specs/2026-05-26-slice-2-log-design.md) — brainstormed binary split (cmd/log-ingester own ports 4327/4328/8083), first-class trace_id+span_id correlation, drift D4 routing consolidation, body substring via positionUTF8 (no text index), packaging B (span scoping + JSON pretty-print). 10 ACs. Next: writing-plans for docs/plans/2026-05-26-slice-2-plan.md."
    },
    {
      "date": "2026-05-26",
      "event": "SLICE-2 execution started — status proposed → building. Subagent-driven mode per docs/plans/2026-05-26-slice-2-plan.md (e1fafa0). 14 tasks; fresh implementer per task + 2-stage review (spec → code quality)."
    },
    {
      "date": "2026-05-26",
      "event": "SLICE-2 done — 14 tasks, all 10 ACs verified. Log vertical: cmd/log-ingester → CH logs_v1 (Row Policy) → /api/v1/logs → /logs page + /traces/:id Logs subtab. Drift D4 closed: nginx /api blocks deleted, Caddy sole ingress. Playwright D4 regression locks the architectural guard. 5 bugs caught during execution. See deploy/SLICE-2-evidence.txt."
    },
    {
      "date": "2026-05-27",
      "event": "SLICE-3 spec landed (docs/specs/2026-05-27-slice-3-services-topology-design.md) — brainstormed 7 design choices: 1min topo-engine background job → 2 tables (topology_edges_v1 + service_stats_v1) with ReplacingMergeTree+FINAL idempotency; full UI scope (Overview cards + Service detail + /topology + /traces/:id Service Map body) MINUS ⌘K which de-scoped to SLICE-3.5; d3-force + hand-rolled SVG (consistent with SLICE-1 waterfall); external nodes via peer.service/db.system/messaging.system/http.host COALESCE; fixed-bucket time window 15m/1h/6h/24h; per-trace Service Map = client-side derive (zero new endpoint). 4th binary cmd/topo-engine on :8084; 2 new Caddy handles; zero nginx change (D4 still closed). 10 ACs. Next: writing-plans for docs/plans/2026-05-27-slice-3-plan.md."
    },
    {
      "date": "2026-05-27",
      "event": "SLICE-3 plan landed (docs/plans/2026-05-27-slice-3-plan.md) — 15 tasks with 100% AC coverage gate. Subagent-driven execution mode chosen. Status proposed → building."
    },
    {
      "date": "2026-05-27",
      "event": "SLICE-3 done — 15 tasks, all 10 ACs verified. Services + topology vertical: cmd/topo-engine (4th binary on :8084) → 1min tick processes closed bucket → CH topology_edges_v1 + service_stats_v1 ReplacingMergeTree tables with FINAL idempotency → /api/v1/services{,/{name}} + /api/v1/topology on cmd/query → 4 new frontend pages (/overview, /services/:name 6-tab, /topology, /traces/:id Service Map subtab body via client-side derive). Shared <ServiceGraph> (d3-force + hand-rolled SVG) reused 3 places. Zero nginx changes (drift D4 stays closed). 9 bugs caught during execution incl. SLICE-3 T15 caught a SLICE-3 T11 regression (services.ts + topology.ts used raw fetch() and skipped the shared axios Bearer interceptor → 401 on every new page; locked by api/__tests__/client.spec.ts). One new known drift D6 (topo-engine AdminConn → activeTenants returns 0 in prod because Row Policy filters tenant discovery; needs CH user exemption — deployment concern, not code correctness). 26/26 Playwright PASS (incl. 13 new SLICE-3 specs); 21 vitest files / 89 tests PASS (+10 from T15 api/auth regression tests). See deploy/SLICE-3-evidence.txt."
    },
    {
      "date": "2026-05-28",
      "event": "PLATFORM-ASK-1/2/3 filed from openaiops-ai SLICE-0 design (sibling repo)"
    }
  ],
  "legend": {
    "priority": "P0=MVP blocker / P1=post-MVP / P2=enhancement",
    "status": "proposed / building / done / done_with_concerns / superseded",
    "effort": "engineer days (ideal, 1-person solo)"
  },
  "features": [
    {
      "id": "SLICE-0",
      "title": "Foundation — repo skeleton + docker-compose stack + multi-tenant gateway shell",
      "priority": "P0",
      "status": "done",
      "completed_at": "2026-05-24",
      "evidence": {
        "ci_run": "https://github.com/huangbaixun/openaiops-platform/actions/runs/26359958968",
        "ci_duration": "6m32s",
        "ac_summary": "8 ACs total: 7 PASS + 1 PARTIAL (AC-7: goose down rollback exists as SQL but not auto-tested in CI; manually verifiable via `goose -dir backend/migrations postgres ... down`).",
        "curl_evidence_file": "deploy/AC-evidence.txt",
        "spec_compliance_runs": "8 spec reviews + 7 code quality reviews dispatched during execution; 1 review-driven fix loop on T1 (vendor gitignore + seed/smoke README accuracy) and 4 CI iteration cycles on T14.",
        "bugs_caught_during_execution": [
          "T13 nginx.conf missing /api proxy block — would have silently 'authenticated' with empty tenant data; caught when CI E2E ran",
          "T14 NaiveUI NInput wraps actual <input> in a div with data-testid; tests needed .locator('input') to drill in",
          "T14 Node 25 stripped Storage.clear(); jsdom shim added"
        ]
      },
      "current_state": "Empty repo just created. No code, no infra, no auth.",
      "target_state": "Engineer can `docker-compose up`, get a stack with PG/Redis/CH/Collector/Caddy/gateway/frontend; curl `/healthz` with a Bearer key returns tenant_id; browser shows Login page (中/英 switchable); CI green.",
      "acceptance_criteria": [
        "docker-compose up brings stack to healthy state (all services)",
        "curl with valid Bearer API key on /healthz returns 200 + tenant_id JSON",
        "curl without / with wrong key returns 401",
        "two tenants seeded; each key resolves to its own tenant (no cross-talk)",
        "Login page renders in zh-CN by default; can switch to en-US",
        "After login, Home page shows tenant name in top bar",
        "goose migrate up creates 3 tables; down rolls back cleanly",
        "GitHub Actions CI (backend / frontend-unit / e2e) is green on push"
      ],
      "out_of_scope": [
        "Trace / Log / Metric ingestion (Slice 1-4)",
        "Topology / Service detail / Alerts (Slice 3-4)",
        "AI agents / RCA (Phase 2 sibling repo openaiops-ai)",
        "OIDC / JWT auth (v1.0)",
        "Production hardening (TLS termination beyond local Caddy, observability of platform itself, etc.)"
      ],
      "dependencies": [],
      "spec": "docs/decisions/0001-initial-architecture.md",
      "plan": "docs/plans/2026-05-24-slice-0-plan.md",
      "estimated_effort_days": "15",
      "actual_effort_session": "1 autonomous subagent-driven session (~3h wall time, 14 task implementer + 11 reviewer + 2 fixer subagent dispatches)",
      "owner": "@huangbaixun",
      "notes": "All 14 tasks in the plan traced to this single feature (per harness-engineering subagent grounding rule). Future slices (SLICE-1 Trace E2E etc.) will get their own feature entries. See ADR-0001 §5 for the slice roadmap.",
      "slice_1_prerequisites": [
        "PRE-1 RESOLVED 2026-05-24: ClickHouse schema migration mechanism — Option B (dedicated ch-migrate service) per ADR-0002. Scaffold + smoke verified.",
        "PRE-2 RESOLVED 2026-05-24: Query API deployment shape — Option B (split cmd/query/ from day 1) per ADR-0003, under Heavy-soon workload assumption. Spec + ADR landed; scaffolding deferred to SLICE-1 T1-T3 sequenced after PRE-3.",
        "PRE-3 RESOLVED 2026-05-24: chquery.MustTenantScope + chquery.Conn (clickhouse-go/v2 wrapper) + Row Policy template (backend/ch-migrations/README.md) + prod CH custom_settings_prefixes config (deploy/clickhouse-custom-settings.xml) + build-time lint (make lint-ch). Cross-tenant reverse E2E (3rd layer of ADR-0001 §3.3) is SLICE-1 AC #8."
      ]
    },
    {
      "id": "SLICE-1",
      "title": "Trace ingest 端到端 — ingester (OTLP receiver) + CH traces_v1 + cmd/query + Traces page",
      "priority": "P0",
      "status": "done",
      "completed_at": "2026-05-26",
      "evidence": {
        "commits_range": "7dfdb22..<T16 SHA>",
        "task_count": 16,
        "ac_summary": "10/10 ACs verified. AC #2/#6 via migrate-ch-up + system.row_policies. AC #1/#3/#4/#5/#9 via Go unit + integration tests. AC #7 via Playwright traces.spec.ts (4 specs PASS in 3.0s). AC #8 via cross_tenant_test.go (8 sub-tests = 6 AC + 2 HTTP regression). AC #10 via .github/workflows/ci.yml (4 jobs).",
        "evidence_file": "deploy/SLICE-1-evidence.txt",
        "bugs_caught_during_execution": [
          "T4/T10/T13 — ingester OTel receiver dropped Bearer headers on BOTH gRPC and HTTP unless `IncludeMetadata: true` is set on each protocol slot. Hot-rod masked the gRPC failure (HTTP-only); cross_tenant_test.go locks both transports.",
          "T1 — CH migration replay-safety: CREATE TABLE and CREATE ROW POLICY must use IF NOT EXISTS so the runner can re-apply after a partial-state crash without a hard error.",
          "T8 review — empty list serialized as JSON null (frontend breakage); fixed by make([]TraceListItem, 0).",
          "T16 — frontend/nginx.conf only proxied /api/* to gateway; the new query-side prefix /api/v1/traces was reachable through Caddy (443) but not through the frontend host port (13000), so the SPA on its own port returned nginx 404. Caught by the first Playwright run; fixed by mirroring Caddy's split in nginx.conf. Tracked as drift D4 for SLICE-2 consolidation."
        ]
      },
      "current_state": "Slice 0 stack runs; no CH tables; no /api/v1/traces endpoint; frontend Traces page does not exist. cmd/query and cmd/ingester binaries do not yet exist. chquery.Conn does not yet expose PrepareBatch (SLICE-1-PREP-1).",
      "target_state": "telemetrygen / hot-r.o.d. → ingester (OTLP/gRPC :4317 + OTLP/HTTP :4318, Bearer-on-receiver) → CH traces_v1 (tenant-scoped, Row Policy active) → curl /api/v1/traces returns list + waterfall JSON via cmd/query :8081 → frontend /traces page renders list + detail waterfall + JSON. Cross-tenant reverse Go integration test asserts tenant B cannot see tenant A's traces.",
      "acceptance_criteria": [
        "ingester OTLP receiver reads Authorization: Bearer <api-key> from gRPC metadata / HTTP header; resolves via auth.PGResolver to obtain authoritative tenant_id; rejects missing or invalid Bearer with 401 + ingester_auth_{missing,invalid}_total counter. SDK-declared tenant.id resource attribute is dropped (cannot be spoofed). [evolved from original Collector-processor framing during 2026-05-25 brainstorming]",
        "CH traces_v1 table created (tenant_id LowCardinality(String), trace_id, span_id, parent_span_id, service, operation, ts DateTime64(9), duration UInt64, status, span_kind, resource_attributes Map(LowCardinality(String),String), attributes Map(LowCardinality(String),String)); ORDER BY (tenant_id, service, ts) PARTITION BY (tenant_id, toYYYYMMDD(ts)); INDEX idx_trace_id bloom_filter; Row Policy tenant_isolation_traces_v1 attached",
        "Go ingester service is itself the OTLP receiver (gRPC :4317 + HTTP :4318, embeds go.opentelemetry.io/collector/receiver/otlpreceiver); writes batches to CH traces_v1 via chquery.PrepareBatch; tenant_id is server-stamped after PG resolve (not propagated from any upstream). Collector is removed from data path. [evolved from original 'receives OTLP from Collector' framing]",
        "Query API: GET /api/v1/traces (list with filters: service/operation/ts_from/ts_to/min_duration_ms, sort ts|duration, limit/offset, has_more pagination) + GET /api/v1/traces/{trace_id} (flat span list for waterfall; 404 if not present under tenant). Both routes live on cmd/query :8081 behind Caddy /api/v1/traces* prefix handle",
        "All Query API SQL goes through chquery.Conn (MustTenantScope helper for Query/Exec; PrepareBatch wrapper for batch writes); make lint-ch CI step rejects bare clickhouse-go imports outside internal/chquery (already enforced from PRE-3 + automatically covers new internal/ingest/ + internal/query/)",
        "ClickHouse Row Policy tenant_isolation_traces_v1 enforces tenant_id = getSetting('custom_tenant_id') at CH level (defense-in-depth on top of builder)",
        "Frontend /traces page (Vue): list view (sortable ts/duration via NDataTable, filter bar) + detail waterfall (hand-rolled SVG, no D3) + JSON subtab. Service Map subtab is a 'Coming in SLICE-3' placeholder; the real force-directed graph component is co-designed with SLICE-3 global topology. [evolved — Service Map subtab body deferred to SLICE-3]",
        "Cross-tenant reverse test as Go integration test (build tag integration) in backend/internal/ingest/cross_tenant_test.go: tenant A ingests via OTLP/gRPC with key-acme, tenant B queries with key-beta → 0 list results + 404 on detail; missing/garbage Bearer → 401; tenant A's own queries see correct counts. Six sub-assertions total.",
        "Per-OTLP-Export-call metering event written to PG metering_events (tenant_id, signal_type='trace', count=spans_in_batch). Write is best-effort: CH commit returns OK to SDK regardless; PG metering failure increments ingester_metering_failed_total + slog.Error. Retry queue is post-SLICE-1. [evolved — best-effort policy made explicit]",
        "Slice 1 CI matrix: backend-unit (existing + new chquery.Batch / internal/ingest / internal/query unit tests) + backend-integration NEW job (-tags=integration -timeout=240s; runs all dockertest suites including cross_tenant) + frontend-unit (+ new Traces vitest specs) + e2e (with new make seed-traces helper + traces.spec.ts Playwright) + lint-ch unchanged"
      ],
      "out_of_scope": [
        "Log ingest (SLICE-2)",
        "Topology / Service detail page (SLICE-3); Service Map subtab body lives there",
        "Metrics ingest + Alerts (SLICE-4)",
        "Sampling decisions (Slice 5+ — for now ingest 100%)",
        "Trace search via attribute index (post-MVP — for now ts-range scan)",
        "AsyncInsert / column-mode batch on chquery.Batch (add when throughput data demands)",
        "Cursor pagination on /api/v1/traces (offset paging ships with documented streaming-drift limitation)",
        "OTLP events / links columns on traces_v1 (cross-jump to logs in SLICE-2 is the planned mitigation)",
        "Proactive 501 stubs for /api/v1/logs|services|topology routes (future slices add their own one-line Caddy handle per CLAUDE.md routing rule)",
        "Collector custom processor (Collector removed from data path entirely; users wanting Collector-style enrichment run their own pointing at ingester:4317)",
        "PGResolver hot-path optimization (Slice 0 known_drift D1 still applies; mitigation if ingester throughput > ~50 req/s)"
      ],
      "dependencies": [
        "SLICE-0",
        "PRE-1",
        "PRE-2",
        "PRE-3"
      ],
      "spec": "docs/specs/2026-05-25-slice-1-trace-design.md",
      "plan": "docs/plans/2026-05-25-slice-1-plan.md",
      "estimated_effort_days": "21",
      "owner": "@huangbaixun",
      "notes": "First vertical slice (Path B). Spec landed 2026-05-25 after brainstorming session; 4 ACs evolved (see acceptance_criteria entries tagged [evolved ...]). Brainstorming closed open task SLICE-1-PREP-1 (chquery.Conn batch path) by deciding to extend the wrapper rather than open a lint exception — implementation tracked in spec §4. Per Slice 0 plan §5 + lessons (docs/lessons-learned-2026-05-24.md): apply Slice 0 NaiveUI test-selector + nginx /api proxy discipline + Go-version pinning. Plan to use telemetrygen for CI determinism + hot-r.o.d. (--profile demo) for manual visual demo."
    },
    {
      "id": "SLICE-2",
      "title": "Log ingest 端到端 — cmd/log-ingester (OTLP receiver) + CH logs_v1 + /api/v1/logs + /logs page with cross-jump",
      "priority": "P0",
      "status": "done",
      "completed_at": "2026-05-26",
      "evidence": {
        "commits_range": "cda3fb8..d954189",
        "task_count": 14,
        "ac_summary": "10/10 ACs verified. AC #2/#6 via CH migration apply + system.row_policies (tenant_isolation_logs_v1 confirmed). AC #1/#3/#5/#8 via Go unit + integration tests (TestSlice2_CrossTenantLogs 8 sub-assertions + TestLogsToRows unit tests). AC #4 via logs_handler_test + logs_repo_test + live curl (10 items returned, beta 0 items). AC #7 via Playwright logs.spec.ts (5/5 PASS in 3.0s) + vitest 52 tests PASS. AC #9 via TestSlice2_MeteringSignalLog integration test (signal_type='log' confirmed). AC #10 via nginx.conf /api blocks deleted + Playwright D4 regression assertion (Content-Type: text/html, no 'items' key) + CI matrix updated.",
        "evidence_file": "deploy/SLICE-2-evidence.txt",
        "bugs_caught_during_execution": [
          "T2 — ingestshared extraction: cmd/ingester imported internal/ingest directly; refactor required careful dep-graph pruning to avoid circular import.",
          "T5 — bodyAsString for plog.ValueTypeMap returned Go-syntax string via .AsString(); needed JSON marshal. Test coverage added for all 6 value type branches.",
          "T7 — body_contains filter used LIKE initially; spec requires positionUTF8. Caught in review; fixed before merge.",
          "T9/T10 — LogsPanel double-fetch: apply()→router.replace→watch→load() double-fetch loop. Fixed by suppressNextWatch flag pattern (same as TracesList). Caught in T9 review.",
          "T11 — Cross-tenant sub2/sub3 passed vacuously: ingest call was inside t.Run for sub1, so sub2/sub3 had no data to deny. Fixed by hoisting ingest before all t.Runs (commit 7ce9741)."
        ]
      },
      "current_state": "SLICE-1 done; trace vertical works (cmd/ingester → traces_v1 → cmd/query → /traces). No log ingest path; no CH logs_v1; no /api/v1/logs route; no /logs frontend; cmd/log-ingester binary does not exist. Drift D4 (Caddy + nginx duplicated routing) still open.",
      "target_state": "SDK / telemetrygen-logs → cmd/log-ingester (OTLP/gRPC :4327 + OTLP/HTTP :4328 /v1/logs, Bearer-on-receiver) → CH logs_v1 (tenant-scoped, Row Policy active) → curl /api/v1/logs with structured filters via cmd/query :8081 → frontend /logs page with severity badges + cross-jump trace_id/span_id chips + JSON body pretty-print. /traces/:id gains a Logs subtab; clicking a span narrows to that span_id. Cross-tenant Go integration test (8 sub-assertions) asserts tenant B cannot see tenant A logs. Drift D4 closed: nginx /api blocks removed; Caddy becomes sole ingress.",
      "acceptance_criteria": [
        "cmd/log-ingester embeds otlpreceiver (gRPC :4327 + HTTP :4328 /v1/logs, IncludeMetadata: true on BOTH transports day 1); reads Authorization: Bearer <api-key>; resolves via auth.PGResolver; server-stamps tenant_id; drops SDK-declared tenant.id resource attribute; rejects missing/invalid with 401 + log_ingester_auth_{missing,invalid}_total counter",
        "CH logs_v1 table created (tenant_id LowCardinality first; ts/observed_ts DateTime64(9); service/severity_text LowCardinality; severity_number UInt8; body String ZSTD(3); trace_id/span_id String with bloom_filter index; trace_flags UInt8; resource_attributes + attributes Map(LC(String),String); minmax index on severity_number); ORDER BY (tenant_id, service, ts); PARTITION BY (tenant_id, toYYYYMMDD(ts)); Row Policy tenant_isolation_logs_v1 attached",
        "internal/logingest.LogConsumer writes batches via chquery.PrepareBatch (*chquery.Batch pins tenant_id at prepare time; first Append arg auto-validated); bodyAsString helper JSON-encodes Map/Slice bodies, AsString for primitives",
        "GET /api/v1/logs on cmd/query :8081 with filters: service (repeated), severity (repeated), ts_from/ts_to (with +1s lookahead drift D1 mitigation), trace_id, span_id, body_contains (positionUTF8), limit/offset; response includes has_more via limit+1 trick; behind Caddy /api/v1/logs* handle",
        "All Query + ingester SQL through chquery.Conn (auto-prepends tenant_id from ctx; never a client URL param); make lint-ch rejects bare clickhouse-go imports outside internal/chquery (already enforced; automatically covers new internal/logingest/ + internal/query/logs_*.go)",
        "Row Policy tenant_isolation_logs_v1 enforces tenant_id = getSetting(custom_tenant_id) at CH level (defense-in-depth on top of MustTenantScope)",
        "Frontend /logs page: list + expand-row JSON (JSON.parse pretty-print of body per packaging B); SeverityBadge component; filter bar (URL is source of truth); /traces/:id gains a Logs subtab via LogsPanel calling /api/v1/logs?trace_id=X; clicking a span in the waterfall narrows to ?trace_id=X&span_id=Y (per packaging B); reverse cross-jump: trace_id/span_id chips in log rows link to /traces/:trace_id?focus_span=Y",
        "Cross-tenant E2E (build tag integration, backend/internal/logingest/cross_tenant_test.go): 8 sub-assertions — A→ingest counts match, B→list read denied, B→trace_id cross-jump denied, A→own trace_id read, A→span_id scoping, gRPC missing-Bearer 401, HTTP missing-Bearer 401 (regression — SLICE-1 T13 IncludeMetadata lesson), garbage-Bearer query 401",
        "Per-OTLP-Export-call metering event written to PG metering_events with signal_type=log, count=records_in_batch; best-effort (CH commit returns OK regardless); log_ingester_metering_failed_total counter for PG failures",
        "Routing consolidated to Caddy-only ingress (drift D4 closed): frontend/nginx.conf /api/* and /api/v1/traces* blocks removed; Caddy gets one new /api/v1/logs* handle; Vite dev proxy + Playwright baseURL repointed to https://localhost (ignoreHTTPSErrors); regression assertion in logs.spec.ts that http://localhost:13000/api/v1/logs returns 404. CI matrix updated: backend-unit (new internal/logingest + internal/query unit tests), backend-integration (new cross-tenant + chtest reuse), frontend-unit (new logs.test.ts + SeverityBadge.test.ts + LogRow.test.ts), e2e (new make seed-logs + logs.spec.ts); lint-ch unchanged"
      ],
      "out_of_scope": [
        "Log retention / TTL (admin concern; SLICE-3+ when tenant settings UI lands)",
        "Full-text body search via tokenbf_v1 / ngrambf_v1 index (revisit when query patterns are real)",
        "Log↔trace bidirectional graph UI (ship cross-jump links + tabs, not a unified timeline view)",
        "OTLP log events / structured normalization (treated as plain body + attributes in v1)",
        "Log-based metrics / extracted spans (post-MVP)",
        "Sampling decisions on logs (ingest 100% in v1)",
        "AsyncInsert / column-mode batch on chquery.Batch (add when throughput data demands)",
        "Cursor pagination on /api/v1/logs (offset paging ships with documented streaming-drift limitation, symmetric to SLICE-1)",
        "Proactive 501 stubs for /api/v1/services|topology|alerts routes (future slices add their own one-line Caddy handle)",
        "Real /api/v1/services endpoint for the filter bar (SLICE-3; SLICE-2 uses type-your-own or auto-suggest-from-recent placeholder)"
      ],
      "dependencies": [
        "SLICE-1"
      ],
      "spec": "docs/specs/2026-05-26-slice-2-log-design.md",
      "plan": "docs/plans/2026-05-26-slice-2-plan.md",
      "estimated_effort_days": "12",
      "owner": "@huangbaixun",
      "notes": "Second vertical slice. Same Bearer-on-receiver pattern as SLICE-1; binary split (new cmd/log-ingester) decided this session — symmetric to PRE-2 cmd/query split for independent blast radius. Internal package refactor: extract internal/ingestshared/ (admin, auth, host, metering, metrics) so internal/ingest/ stays trace-only and new internal/logingest/ stays log-only. Drift D4 bundled: nginx /api blocks deleted, Caddy becomes sole ingress (closes 2026-05-26 lessons-learned D4). Packaging B chosen — adds span-granularity scoping (?span_id=X) and JSON body pretty-print on top of the minimal mirror-SLICE-1 baseline."
    },
    {
      "id": "SLICE-3",
      "title": "Services + Topology — cmd/topo-engine + 2 CH aggregation tables + 3 new Query API endpoints + 4 new frontend pages",
      "priority": "P0",
      "status": "done",
      "completed_at": "2026-05-27",
      "evidence": {
        "commits_range": "7f0f09d..3308b9b",
        "task_count": 15,
        "ac_summary": "10/10 ACs verified. AC #2/#3 via CH SHOW CREATE TABLE + system.row_policies (both tenant_isolation_topology_edges_v1 + tenant_isolation_service_stats_v1 attached). AC #1/#4 via Go integration tests (TestTopoEngine_RunOnce_WritesEdgesAndStats, Idempotency_DoubleRun, Catchup_FromLastCompleted, CannotWriteEdgeAcrossTenant — all PASS via dockertest). AC #5/#6/#7 via services_repo_test + topology_repo_test + handler unit tests + live curl through Caddy. AC #8 via Caddy handle blocks present + zero nginx changes (D4 stays closed) + live verification curl through https://localhost. AC #9 via vitest 89/89 (incl. ServiceGraph + TimeWindowPicker suppressNextWatch + Overview + ServiceDetail + Topology + ServiceMapPanel + 3 new api regression specs) + Playwright topology/overview/service-detail specs (13 specs). AC #10 via TestSlice3_CrossTenantTopology 6 sub-assertions + write isolation + idempotency + Playwright cross-tenant assertions for all 4 new pages.",
        "evidence_file": "deploy/SLICE-3-evidence.txt",
        "bugs_caught_during_execution": [
          "T1 review: topology_edges_v1 ORDER BY missing caller_kind → would silently dedupe future caller_kind values. Spec was wrong; corrected in commit a224ddb.",
          "T2 review: AdminConn godoc lied (claimed no setting injection but adminCtx injected empty sentinel). Fixed in commit eda45e5.",
          "T3 review: tenants.go silent skip on bad UUID + redundant TickFailedTotal counter + state.go io.EOF swallowed indistinguishably. Fixed in commit 30b2ce6.",
          "T4: plan UUIDs were invalid 4-4-4-4-12 format (should be 8-4-4-4-12). Implementer caught + fixed.",
          "T9: Pre-existing make lint-ch failure on cmd/topo-engine/main.go (rule too narrow). Fixed by extending lint Rule 2 to allow cmd/topo-engine/ subsystem. Commit d005814.",
          "T10: forceLink in d3-force needs source/target fields; GraphEdge clone projection added in useForceSimulation.",
          "T11: vue-router test harness needs await router.isReady() before mount; @/ alias not configured so tests use relative imports.",
          "T13: useForceSimulation TS2344 from T10 baseline; GraphNode/SimEdge now extend SimulationNodeDatum/SimulationLinkDatum via SimEdge intermediate type. Commit abff6f4.",
          "T15 (this commit): services.ts + topology.ts originally landed in T11 using raw fetch() instead of the shared api/client.ts axios instance. The shared client's request interceptor (auto-injects Bearer from localStorage.apiKey) was therefore skipped — every /overview / /topology / /services/:name page hit 401. Caught only when SLICE-3 T15 Playwright run rendered 'services 401' NAlert. Fixed by switching both API modules to client.get(); regression locked by new api/__tests__/client.spec.ts (interceptor injects header from localStorage) + updated services.spec.ts + topology.spec.ts (assert client.get('/v1/...'), not fetch('/api/v1/...'))."
        ]
      },
      "current_state": "SLICE-3 DONE 2026-05-27. cmd/topo-engine 4th binary running on :8084; topology_edges_v1 + service_stats_v1 ReplacingMergeTree tables with FINAL queries; 3 new query API endpoints; 4 new frontend pages with shared <ServiceGraph>. 26 Playwright specs PASS; 89 vitest tests PASS; backend integration suite PASS. Known drift D6: AdminConn → activeTenants returns 0 in prod due to Row Policy filter on traces_v1 — needs CH user exemption (deployment concern, not code correctness; integration tests already use exempt CH user).",
      "target_state": "cmd/topo-engine runs as 4th docker-compose service on :8084 admin; every 1min processes the latest closed bucket [T-2min, T-1min); reads traces_v1 via SELF JOIN; writes topology_edges_v1 (per pair) + service_stats_v1 (per service x span_kind). Cold-start Catchup replays missing buckets up to TOPO_CATCHUP_MAX=1h gap. cmd/query :8081 gains /api/v1/services (list+RED), /api/v1/services/{name} (stats+inbound+outbound deps), /api/v1/topology (nodes+edges for force-directed graph). Caddy adds 2 handle blocks (zero nginx change). Frontend ships /overview (service cards, RED color rings), /services/:name (NTabs: Signals + Dependencies + 4 ComingSoonTab placeholders), /topology (global force-directed graph via d3-force + hand-rolled SVG, ≤100 nodes), /traces/:id Service Map subtab body (client-side derived from in-memory spans). Shared <ServiceGraph> component reused 3 places. ?window= URL param drives 15m/1h/6h/24h fixed buckets across all 3 new pages. SideBar overview/services/topology entries enabled. Cross-tenant integration test (6 sub-assertions) asserts tenant B cannot see tenant A's services or topology.",
      "acceptance_criteria": [
        "cmd/topo-engine on :8084 admin port; every 1min processes closed bucket [T-2min, T-1min); cold-start Catchup replays missing buckets capped at TOPO_CATCHUP_MAX (default 1h); per-tenant errgroup limit TOPO_TENANT_CONCURRENCY (default 4); single-tenant failure does not abort tick; topo_engine_bucket_lag_seconds gauge + topo_engine_tick_failed_total + topo_engine_tenant_failed_total{tenant_id} counters exposed at :8084/metrics; /healthz returns 200 once Catchup completes (gate is Catchup-complete, not first-tick-complete; Catchup already replays all closed buckets up to now)",
        "CH topology_edges_v1 table (tenant_id LowCardinality first; ts_bucket DateTime with Delta+ZSTD; caller_service/caller_kind/callee_service/callee_kind LowCardinality(String); calls/errors/p95_duration UInt64 T64+LZ4; ENGINE=ReplacingMergeTree; ORDER BY (tenant_id, ts_bucket, caller_service, caller_kind, callee_service, callee_kind) — caller_kind included in dedup key for forward-compat with non-'service' values; PARTITION BY (tenant_id, toYYYYMMDD(ts_bucket))); Row Policy tenant_isolation_topology_edges_v1 attached with USING tenant_id = getSetting('custom_tenant_id') TO openaiops",
        "CH service_stats_v1 table (tenant_id LowCardinality first; ts_bucket DateTime with Delta+ZSTD; service/span_kind LowCardinality(String); calls/errors/p95_duration UInt64; ENGINE=ReplacingMergeTree; ORDER BY (tenant_id, ts_bucket, service, span_kind)); Row Policy tenant_isolation_service_stats_v1 attached",
        "internal/topoengine.Engine.RunOnce(ctx, bucket) runs two Passes per tenant: Pass A writes topology_edges_v1 (LEFT JOIN traces_v1 self with SETTINGS join_use_nulls=1 so IS NULL checks fire; internal edge when a.service IS NOT NULL AND a.service != b.service [caller=a.service, callee=b.service, callee_kind=service]; external edge when b.span_kind='Client' AND b.parent_span_id != '' AND (a.service IS NULL OR a.service = b.service) AND has peer attribute [caller=b.service, callee derived from nullIf+coalesce over peer.service / db.system / messaging.system / http.host / 'unknown-external', callee_kind=external]; multiIf branches on the same internal-vs-external predicate for caller_service / callee_service / callee_kind to ensure consistency); Pass B writes service_stats_v1 (GROUP BY service, span_kind). Both Passes use chquery.Conn (Layer 1 SQL WHERE tenant_id + Layer 2 Row Policy + Layer 3 chquery custom_tenant_id setting). New chquery.AdminConn type bypasses MustTenantScope for whitelisted SQL (AdminListTenants, AdminMaxBucket); make lint-ch rule extended to forbid chquery.AdminConn construction outside internal/topoengine/",
        "GET /api/v1/services on cmd/query :8081 with window enum 15m|1h|6h|24h (handler whitelist → INTERVAL N MINUTE/HOUR); returns items[].{service, inbound_calls, inbound_errors, inbound_error_rate, inbound_p95_ms, outbound_calls}; sumIf/maxIf over service_stats_v1 FINAL filtered by span_kind='Server' for inbound and 'Client' for outbound; HAVING inbound_calls>0 OR outbound_calls>0; ORDER BY inbound_calls DESC; LIMIT default 100 max 500",
        "GET /api/v1/services/{name}?window=... returns stats.{inbound, outbound} + dependencies.{inbound[], outbound[]}; self stats from service_stats_v1 FINAL grouped by span_kind; inbound deps from topology_edges_v1 FINAL where callee_service=name AND callee_kind='service'; outbound deps from topology_edges_v1 FINAL where caller_service=name; each dependencies array LIMIT 50; 404 when service has neither stats nor edges in window",
        "GET /api/v1/topology?window=...&node_limit=N returns nodes[].{service, kind, calls, errors, p95_ms} + edges[].{caller, callee, callee_kind, calls, errors, p95_ms}; top_services CTE filters by sum(calls) DESC LIMIT node_limit (default 100, max 300); external nodes (callee_kind='external') never truncated; empty window returns 200 + {nodes:[], edges:[]}",
        "Caddy gets /api/v1/services* + /api/v1/topology* handle blocks (uri strip_prefix /api + reverse_proxy query:8081) listed before catch-all /api/* handle; zero changes to frontend/nginx.conf (drift D4 closed in SLICE-2 holds); CLAUDE.md port table updated to add 8084 topo-engine canonical port; deploy/.env.example adds TOPO_ENGINE_ADMIN_HOST_PORT=8084",
        "Frontend ships 4 pages: /overview (ServiceCard grid with RED color ring, NTabs window picker), /services/:name (NTabs shell with SignalsTab + DependenciesTab + 4 ComingSoonTab placeholders for Runtime/Exceptions/Settings/Alerts), /topology (TopologyPage with shared <ServiceGraph>, ≤100 nodes), /traces/:id Service Map subtab body (ServiceMapPanel.vue client-side derives nodes+edges from already-loaded spans via Vue computed, zero backend call, zero external node inference). Shared <ServiceGraph> component reused in all 3 graph locations (uses d3-force ~30KB gz simulation + hand-rolled Vue SVG render). <TimeWindowPicker> URL ?window= as single source of truth + suppressNextWatch composable pattern (SLICE-2 LogsPanel T9/T10 lesson reused). SideBar overview/services/topology entries change from to:null to real paths (services entry shares /overview).",
        "Cross-tenant integration test (backend/internal/topoengine/cross_tenant_test.go, build tag integration): 6 sub-assertions — A sees own topology with checkout/redis nodes, B sees own topology with mobile/orders, B GET /services/{A-service-name} returns 404, B GET /services list excludes all A services, missing Bearer on all 3 new routes returns 401, garbage Bearer on all 3 new routes returns 401. seedSpansForTenant and eng.RunOnce calls HOISTED outside t.Run blocks per SLICE-2 T11 lesson. Plus 4 additional integration tests: TestTopoEngine_RunOnce_WritesEdgesAndStats, TestTopoEngine_Idempotency_DoubleRun (ReplacingMergeTree FINAL dedupes), TestTopoEngine_Catchup_FromLastCompleted, TestTopoEngine_CannotWriteEdgeAcrossTenant (reverse write isolation). CI matrix: backend-unit (engine/handler/admin unit tests), backend-integration (5 new integration files), frontend-unit (8 new vitest spec files including TimeWindowPicker suppressNextWatch regression), e2e (make seed-topology + topology.spec.ts + overview.spec.ts + service-detail.spec.ts); lint-ch rule extended for chquery.AdminConn usage location"
      ],
      "out_of_scope": [
        "⌘K command palette + services candidates injection (skeleton ADR-0001 §4.4 promised in Slice 0 was YAGNI'd; building skeleton in SLICE-3 adds ~3 tasks — recommend folding into SLICE-3.5 or SLICE-5)",
        "Service detail tabs 3-6 (Runtime/Exceptions/Settings/Alerts) — depend on SLICE-4 metrics and SLICE-5 tenant settings; SLICE-3 ships ComingSoonTab placeholders",
        "True windowed quantile (vs maxIf(p95) per-bucket-max approximation) — requires quantileTDigestState columns in topo-engine writes; folded into SLICE-4 metrics aggregation rework",
        "Separate /services table view — SideBar services shares /overview cards; sortable table view deferred to SLICE-3.5",
        "/api/v1/services/{name}/traces and /logs quick-link endpoints — not needed; SignalsTab links directly to /traces?service=X (filter already supported by SLICE-1/2)",
        "topo-engine HA / leader election — single instance suffices; ReplacingMergeTree tolerates restart re-runs",
        "OPTIMIZE TABLE ... FINAL scheduled job — does not run (too costly); FROM ... FINAL in queries guarantees correctness",
        "Topology node hover tooltip with inbound/outbound split — v1 hover shows current node RED summary only; click navigates to /services/:name for breakdown",
        "Topology time-animation / sliding-window replay — fixed-bucket TimeWindowPicker satisfies ADR-0001 'time window switch with number replay'; continuous animation deferred",
        "External node reverse navigation (external node → trace_id) — external nodes don't map to specific traces; UX deferred",
        "Per-trace Service Map external derivation — peer-attribute inference unstable within single trace; external nodes appear only in global /topology",
        "Proactive 501 stubs for /api/v1/alerts — SLICE-4 adds its own one-line Caddy handle",
        "AsyncInsert / column-mode batch on chquery.Batch — add when throughput data demands",
        "OTel SDK attribute alias normalization (peer.service vs net.peer.name vs server.address) — known_drift for SLICE-3.5"
      ],
      "dependencies": [
        "SLICE-1",
        "SLICE-2"
      ],
      "spec": "docs/specs/2026-05-27-slice-3-services-topology-design.md",
      "estimated_effort_days": "21",
      "owner": "@huangbaixun",
      "notes": "Third vertical slice. First slice with a non-OTLP-receiving backend service (cmd/topo-engine). Introduces chquery.AdminConn for the tenant-discovery case (whitelisted SQL only, lint-confined to internal/topoengine/). External node inference via COALESCE on peer attributes is the second-biggest design choice after the 1min job-vs-MV decision. Drift D4 stays closed: zero nginx changes (this is the first slice that proves SLICE-2's promise that future query routes are one-line Caddy adds). ⌘K explicitly de-scoped during brainstorming after discovering the cmd palette skeleton was never built — Q2 'full scope' answer carried the assumption it existed. SLICE-3 plan should target 14-16 tasks (similar to SLICE-1's 16); 21-day estimate (vs ADR-0001's 5-week estimate) reflects two-Pass topo-engine job + shared graph component complexity offset by SLICE-1/2 helpers (chtest, suppressNextWatch pattern, ingestshared) being reusable."
    },
    {
      "id": "PLATFORM-ASK-1",
      "name": "service:ai API key scope + X-Tenant-Id header trust",
      "priority": "P1",
      "status": "done",
      "spec": null,
      "evidence": "AC#1: scope column already shipped Slice-0 (backend/migrations/20260524000002_create_api_keys.sql, TEXT NOT NULL DEFAULT 'read-write') — no new migration needed. AC#2/#3/#4: auth boundary logic implemented in auth.Middleware (ScopeServiceAI gate + X-Tenant-Id resolution via new auth.TenantLookup interface) and verified by 5 new unit tests in internal/auth/middleware_test.go: ServiceAI_HonorsTenantHeader (adopts target tenant), NonServiceAI_IgnoresTenantHeader (pinned to key), ServiceAI_NoHeader_PinsToKeyTenant, ServiceAI_MalformedTenantHeader_400, ServiceAI_UnknownTenant_404. PGResolver.TenantByID added (+ErrTenantNotFound) with integration test TestPGResolver_TenantByID. service:ai seed key added (deploy/seed.sql, plaintext test-key-ai, tenant ai-svc 9999...). VERIFIED 2026-05-30 (docker up): dockertest integration `go test -tags=integration ./internal/auth/` PASS (4.2s); full unit suite PASS; live cross-tenant 403 confirmed end-to-end through cmd/query (curl https://localhost/api/v1/annotations with acme key + beta body tenant → 403).",
      "source": "Derived from openaiops-ai SLICE-0 spec §5.3 + §11.",
      "description": "Add scope column to api_keys table. Gateway honors X-Tenant-Id header only when bearer has scope='service:ai'. Lets openaiops-ai operate cross-tenant with a single secret.",
      "acceptance_criteria": [
        "api_keys table has scope column (text NOT NULL); migration applied",
        "scope='service:ai' key + X-Tenant-Id: <T> header against /api/v1/traces returns tenant T data",
        "scope='tenant:<X>' key + X-Tenant-Id header is ignored (tenant pinned to key)",
        "Regression: existing tenant-scoped keys continue working unchanged"
      ],
      "out_of_scope": ["per-key rate limits (separate ask)", "key management UI"],
      "dependencies": [],
      "technical_notes": "Aligns with platform spec §3.3. Slice-1 blocker for openaiops-ai. Without it, AI falls back to N per-tenant keys."
    },
    {
      "id": "PLATFORM-ASK-2",
      "name": "POST /api/v1/annotations endpoint",
      "priority": "P1",
      "status": "done",
      "spec": "docs/specs/2026-05-29-platform-ask-2-annotations-design.md",
      "plan": "docs/plans/2026-05-29-platform-ask-2-annotations-plan.md",
      "evidence": "8 TDD tasks, subagent-driven (commits b157cb7..189363f). PG annotations table (migration 20260529120000) + AnnotationsRepo (Insert with NULLIF idempotency + ON CONFLICT, List tenant-scoped) on cmd/query; POST/GET /v1/annotations handler; router gains db param; Caddy /api/v1/annotations* handle before gateway catch-all. Frontend: api/annotations.ts (shared client), useAnnotations composable, AnnotationBadge.vue, wired into TraceDetail (props.traceId), ServiceDetail (route.params.name), ServiceGraph node marker (n.service). Final code review (no Critical) findings I-1/M-1/N-2 fixed in 189363f. VERIFIED 2026-05-30 (docker up): dockertest integration `go test -tags=integration ./internal/query/` PASS (6.9s); unit suite PASS; frontend build + vitest 96 PASS; live stack `make up && make seed && make smoke` PASS; curl through Caddy → AC#1 201+annotation_id, AC#3 cross-tenant 403, AC#4 idempotent 200; full Playwright e2e 28/28 PASS incl. annotations.spec (badge on service-detail + cross-tenant 403). NOTE: first e2e run 404'd because `make up` does not --build and reused stale query+frontend images predating the route — fixed by rebuilding both; lesson recorded (drift D7 closed). e2e still asserts only service-detail badge, not trace/topology (drift D8, open).",
      "source": "Derived from openaiops-ai SLICE-0 spec §5.1 + §11.",
      "description": "Endpoint for openaiops-ai to write back RCA findings as annotations on trace/service/topology nodes. Display in platform UI when present.",
      "acceptance_criteria": [
        "POST /api/v1/annotations with {tenant_id, target_type, target_id, kind: 'ai_rca', payload, ts} returns 201 + annotation_id",
        "Annotation visible as badge on the corresponding trace/service/topology node in UI",
        "Cross-tenant write blocked (annotation tenant_id must match bearer scope or X-Tenant-Id header)",
        "Idempotency-Key header dedupes within 24h"
      ],
      "out_of_scope": ["UI editing of annotations", "annotation comments/threads"],
      "dependencies": ["PLATFORM-ASK-1"],
      "technical_notes": "Used by openaiops-ai Slice 1 write-back. Spec §6 contract."
    },
    {
      "id": "PLATFORM-ASK-3",
      "name": "Notification channel type=ai-rca",
      "priority": "P1",
      "status": "proposed",
      "spec": null,
      "source": "Derived from openaiops-ai SLICE-0 spec §5.1 + §11.",
      "description": "Within platform SLICE-4 (alerts), add notification channel kind 'ai-rca' that POSTs Alertmanager v2 payload to openaiops-ai /webhooks/alertmanager. Configurable per tenant.",
      "acceptance_criteria": [
        "notification_channels table accepts type='ai-rca' + target_url + per-channel api_key",
        "When alert fires, payload POSTed with bearer + Idempotency-Key=alert_fingerprint",
        "Delivery failures retried with exponential backoff; permanent failure surfaced in audit_log",
        "Regression: existing Slack/SMTP channels work unchanged"
      ],
      "out_of_scope": ["AI receiving non-alert webhooks", "two-way ack from AI"],
      "dependencies": [],
      "technical_notes": "Lives within platform SLICE-4 scope; do not promote to its own slice."
    },
    {
      "id": "PLATFORM-UI-1",
      "name": "Frontend UI refactor (align to OpenAPM hi-fi design)",
      "priority": "P2",
      "status": "done",
      "spec": "docs/specs/2026-05-30-platform-ui-1-refactor-design.md",
      "plan": "docs/plans/2026-05-30-platform-ui-1-refactor-plan.md",
      "evidence": "11 TDD tasks, subagent-driven (commits cae5df0..7dadb0c, ff-merged to main). T1 port OpenAPM core.css/components.css globally. Shell rebuilt as SFCs: useTheme (data-theme+persist), useSidebar (collapse+persist), grouped 3-section sidebar w/ icons + disabled '即将上线' placeholders for 9 backend-less pages, ScopePill (Project=tenant real; Domain/Env read-only w/ FEATURE-B/PLATFORM-MT-1 seam), ⌘K CommandPalette (fuzzy pages+live services), TopBar (logo/scope-pill/time-range/auto-refresh/theme/lang/avatar+logout), AppLayout grid + global ⌘K listener. useTimeWindow extended w/ intervalSec/refreshTick/setRefreshInterval; 5 data pages auto-refresh. 6 real pages restyled to OpenAPM vocabulary (.card/.kpi/.tbl/.badge/.pfilter/.section-h/.waterfall) with ALL data-testids preserved. Each task 2-stage reviewed (spec then quality); final holistic review READY_TO_MERGE (presentational-only invariant verified: zero changes under src/api|stores|router or backend/). VERIFIED 2026-05-31: 116 vitest + npm run build (vue-tsc) clean + 31/31 Playwright e2e (28 existing + 3 new shell: ⌘K nav, theme persist, sidebar collapse) against freshly rebuilt frontend image. Topo-engine service_stats empty during e2e was a timing artifact (topo-engine ~120s bucket lag), masked in dev by CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1; the prod-only Row Policy admin bug is pre-existing drift D6, not introduced here.",
      "source": "User request 2026-05-30: refactor frontend UI to follow https://huangbaixun.github.io/OpenAPM/ (local clone /Users/huangbaixun/code_space/OpenAPM/).",
      "description": "Feature A of a two-feature split. Port OpenAPM's shell chrome + component vocabulary (core.css/components.css) into the Vue app and restyle the six backend-wired pages (Overview/Traces/Logs/Topology/Service/Login) to match. Purely presentational: no api/composables/stores/router/backend changes. Adds theme toggle, ⌘K command palette, topbar time-range + auto-refresh, grouped collapsible sidebar. Topbar Domain/Project/Env selectors are built as a forward-compatible presentation layer (Project=current tenant real; Domain/Env read-only) to be wired by Feature B.",
      "acceptance_criteria": [
        "Sidebar renders OpenAPM's 3 nav groups with icons; 6 real pages navigate; backend-less items are disabled '即将上线' placeholders; collapse state persists",
        "TopBar renders logo+brand+scope-pill+time-range+refresh-group+theme-toggle+language+avatar user-menu; logout works",
        "Theme toggle switches light/dark via data-theme and persists across reload",
        "⌘K command palette fuzzy-jumps to any page and to any service from the live list",
        "Time-range control drives the 6 pages' existing query window; auto-refresh re-queries on interval",
        "All 6 real pages restyled to OpenAPM vocabulary with every existing data-testid intact",
        "Scope-pill shows Project=current tenant; Domain/Env present as read-only forward-compatible selectors with documented Feature-B seam",
        "Existing 96 vitest + 28 Playwright remain green; npm run build passes; new shell unit/e2e tests pass"
      ],
      "out_of_scope": [
        "Real Domain/Env/tenant switching + any backend change (-> PLATFORM-MT-1, Feature B, with ADR)",
        "The 9 backend-less OpenAPM mock pages with fake data",
        "design-annotations (?) overlay; mobile/responsive"
      ],
      "dependencies": [],
      "technical_notes": "OpenAPM-CSS-first hybrid (DESIGN-DISCUSSION.md warns against antd/shadcn spacing). Keep NaiveUI only for NSpin/NModal/NSelect, themed. Pure presentational refactor => existing test suite is the regression guard. Forward-references PLATFORM-MT-1 for scope-pill wiring."
    },
    {
      "id": "PLATFORM-MT-1",
      "name": "Domain-scoped multi-tenant switching",
      "priority": "P2",
      "status": "done",
      "spec": "docs/specs/2026-05-31-platform-mt-1-domain-switching-design.md",
      "plan": "docs/plans/2026-05-31-platform-mt-1-domain-switching-plan.md",
      "adr": ["0004"],
      "evidence": "7 TDD tasks, subagent-driven (commits c35bfab..bbcd38c, ff-merged to main). T1 migration (domains, tenants.domain_id/environment, audit_log). T2 tenant.Tenant DomainID/Environment + PGResolver. T3 middleware `domain` scope branch (in-domain X-Tenant-Id adopt / out-of-domain 403 / non-domain ignores) + ctx scope/keyid; final review confirmed 5 fail-closed security checks PASS (NULL-domain can't switch, Nil!=Nil, service:ai unchanged, read-write ignores header, non-TenantLookup->404). T4 GET /v1/tenants (domain peers/single, NULL-domain no over-disclosure via PG x=NULL semantics) + POST /v1/tenants/switch (validate + audit once, NOT per-request) on gateway. T5 frontend tenants api + auth store activeTenantId/switchActiveTenant + X-Tenant-Id interceptor. T6 ScopePill live switcher. T7 seed demo-domain + e2e. Two real bugs caught by e2e + fixed (routes /api/v1->/v1 for Caddy strip-prefix). Final holistic review READY_TO_MERGE: chquery data layer ZERO-DIFF (isolation mechanism untouched), invariant enforced redundantly at middleware (real gate) + /switch (audit pre-flight). VERIFIED 2026-05-31: backend unit+integration (auth/identity/query) green, 120 vitest, build clean, MT-1's 3 e2e green (in-domain switch / single read-only / out-of-domain 403), cross-tenant isolation e2e GREEN (A-writes/B-reads->0 preserved). CONCERN: 1 pre-existing e2e red — shell.spec ⌘K (a UI-1 test) fails on D6 (topo-engine admin activeTenants() blocked by Row Policy -> service_stats empty -> palette empty); MT-1 touched no CH/topo code; latently red on main too. User approved landing MT-1 + tracking D6 separately (see known_drift D6).",
      "source": "User request 2026-05-30: full multi-tenant switching behind the OpenAPM topbar scope selectors. Decomposed out of PLATFORM-UI-1 as the security-critical backend half. Identity substrate locked 2026-05-31: domain-scoped API keys (NOT human users/sessions).",
      "description": "Feature B. Add domain grouping + per-tenant environment tag (goose migration), and a new 'domain' api_keys scope that may switch among the tenants of its own domain via the existing X-Tenant-Id mechanism (generalized from service:ai, fail-closed on domain membership). New GET /api/v1/tenants enumerates the caller's visible tenants. Wires the PLATFORM-UI-1 scope-pill to real switching (same key + header, no re-login). Preserves the one-active-tenant-per-request isolation invariant (ADR-0001 §3.3) — MustTenantScope + Row Policy unchanged. No human-user/password/session system (ADR-0004).",
      "acceptance_criteria": [
        "domains table + tenants.domain_id + tenants.environment migrated (goose up/down); existing tenants survive with NULL domain_id",
        "A 'domain'-scoped key switches among tenants of its own domain via X-Tenant-Id; out-of-domain target -> 403; absent header -> own tenant; read-write/service:ai behavior unchanged",
        "GET /api/v1/tenants returns the caller's domain peers (domain key) or its single tenant (other), each {id,name,environment}",
        "PLATFORM-UI-1 ScopePill wired: Project dropdown switches active tenant (no re-login), Env shows/groups by environment, pages re-query under new tenant",
        "Isolation invariant preserved: reverse E2E (in-domain switch sees only target; out-of-domain blocked; A-writes/B-reads => 0) green in CI",
        "ADR-0004 written documenting the auth-model change; each switch writes an audit_log row",
        "All existing tests stay green; new unit/integration/e2e pass"
      ],
      "out_of_scope": ["Human users/passwords/SSO/sessions/JWT", "domain & membership management UI (seed/migration-managed)", "cross-domain switching; environment-based access control", "per-service ACL; team/squad aggregation tier"],
      "dependencies": ["PLATFORM-UI-1", "PLATFORM-ASK-1"],
      "technical_notes": "Security-critical: changes who can see which tenant (CLAUDE.md day-1 load-bearing invariant). Identity substrate = domain-scoped API keys (light), not human users. Generalizes ASK-1's X-Tenant-Id from service:ai to 'domain' scope, validated by shared non-NULL domain_id. ADR-0004 + spec authored 2026-05-31. Switch is auth-time only; data layer (MustTenantScope/Row Policy) untouched."
    },
    {
      "id": "PLATFORM-TOPO-1",
      "name": "topo-engine PG-driven tenant discovery (fix D6)",
      "priority": "P2",
      "status": "proposed",
      "spec": "docs/specs/2026-05-31-platform-topo-1-pg-tenant-discovery-design.md",
      "plan": "docs/plans/2026-05-31-platform-topo-1-pg-tenant-discovery-plan.md",
      "adr": ["0005"],
      "source": "Fixes known_drift D6 (topo-engine admin discovery blocked by Row Policy -> empty service_stats). Approach C (PG-driven) chosen by user 2026-05-31 over A (Row-Policy-exempt CH user).",
      "description": "topo-engine discovers tenants from the authoritative PG tenants table (via its already-open, unused Deps.PG) instead of the AdminConn sentinel path that the tenant_isolation_* Row Policy filters to 0 rows. lastCompletedBucket becomes a tenant-scoped CH query. chquery.AdminConn + sentinel + its lint exception are removed. Per-tenant aggregation (edges.go/services.go) is unchanged. No Row Policy DDL / CH user / deploy change. Strengthens isolation by removing the only Row-Policy-bypass mechanism. ADR-0005.",
      "acceptance_criteria": [
        "activeTenants sources the tenant list from PG (tenants table) via Deps.PG; AdminConn-based discovery is gone",
        "lastCompletedBucket is a tenant-scoped CH query (passes Row Policy, returns the real max bucket)",
        "chquery.AdminConn + sentinel + lint exception removed; topo-engine builds; make lint-ch passes",
        "On a fresh stack (policy enforced for openaiops), after seed + one tick, service_stats_v1/topology_edges_v1 populate and GET /api/v1/services returns data",
        "shell.spec ⌘K e2e passes; full Playwright suite green; known_drift D6 closed",
        "No Row Policy DDL / CH user / deploy-config change; ADR-0005 documents the decision"
      ],
      "out_of_scope": ["Row-Policy-exempt CH user (Option A, rejected)", "idle-tenant aggregation optimization", "aggregation SQL changes"],
      "dependencies": [],
      "technical_notes": "topo-engine already opens PG (cmd/topo-engine/main.go:55, Deps.PG) but never uses it. Both broken admin queries (activeTenants via AdminListTenants, lastCompletedBucket via AdminMaxBucket) move off AdminConn. Test CH user has ACCESS_MANAGEMENT=1 which masks the Row Policy, so the real D6 proof is the live ⌘K e2e on the dev stack. Fixes the issue that bit UI-1 + MT-1 e2e."
    }
  ]
}
