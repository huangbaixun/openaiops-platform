{
  "schema_version": "1.0",
  "description": "Cross-session progress tracker for Claude Code agent work on openaiops-platform. Pairs with features.json (status ledger) and lessons-learned (sediment).",
  "updated_at": "2026-05-24",
  "active_branch": "main",
  "current_focus": "Slice 0 (Foundation) DONE 2026-05-24. Pre-Slice-1 phase: 3 prerequisite decisions (PRE-1/2/3 in open_tasks) must be resolved before ANY ClickHouse-writing code. The first one to decide is PRE-1 (CH schema init mechanism) — it gates how SLICE-1's first migration is written.",
  "open_tasks": [
    {
      "id": "PRE-1",
      "title": "Decide ClickHouse schema init mechanism",
      "priority": "P0",
      "blocks": "SLICE-1",
      "options": [
        "(a) docker-entrypoint-initdb.d/ — simplest, but ties to fresh-container init; no story for schema migration on running cluster",
        "(b) ch-migrate service symmetric to PG goose, using clickhouse-client --queries-file — heavier setup but enables future migrations"
      ],
      "recommendation": "Option (b) for symmetry + future-proofing. Start as a one-shot like the PG migrate; later upgrade to a goose-like sequencer when CH schema starts changing post-creation."
    },
    {
      "id": "PRE-2",
      "title": "Decide Query API deployment shape",
      "priority": "P0",
      "blocks": "SLICE-1",
      "options": [
        "(a) Extend gateway binary — one Dockerfile, shared middleware, fast to ship",
        "(b) Separate cmd/query/ service per ADR-0001 §3.2 — independent scaling, cleaner boundary"
      ],
      "recommendation": "Option (a) for Slice 1 (single binary suffices at pre-prod scale); split when load testing shows gateway-vs-query traffic patterns diverge meaningfully. Cost of splitting later is moderate (1-2 days)."
    },
    {
      "id": "PRE-3",
      "title": "Implement MustTenantScope query builder + CH Row Policy + cross-tenant E2E reverse tests",
      "priority": "P0",
      "blocks": "SLICE-1 first CH write",
      "notes": "ADR-0001 §3.3's 3-layer multi-tenant safeguard. Must land BEFORE any code writes/queries CH with real (non-test-fixture) data. The MustTenantScope helper is the API the rest of the codebase will rely on — design it carefully."
    }
  ],
  "recently_completed": [
    {
      "date": "2026-05-24",
      "name": "Slice 0 (Foundation) — full delivery",
      "summary": "Bootstrap → Go backend (1.25, chi v5, pgx v5, goose v3) → PG schema (tenants/api_keys/metering_events) → bcrypt + tenant domain types → ApiKey pgx repo (dockertest integration tests) → auth Bearer middleware (ctx.tenant_id injection) → PG resolver + chi gateway server + /healthz → docker-compose stack (PG+Redis+CH+OTel-Collector+Caddy+gateway+frontend, 8080/4317/4318 env-overridable for SignOz collision) → Vue 3 + Vite + Pinia + Naive UI scaffold → openapm design tokens → vue-i18n (zh-CN/en-US) + auth store (TDD) + Vue Router with guard + LoginView + HomeView + AppLayout + TopBar + SideBar → frontend Dockerfile + nginx (with critical /api proxy) + Caddy reverse-proxy → Playwright E2E (4 specs incl. two-tenant cross-context) + GitHub Actions CI (3 jobs: backend / frontend-unit / e2e). CI green 6m32s on run 26359958968. 8/8 ACs verified (AC-7 partial). 14 implementer + 11 reviewer + 2 fixer subagent dispatches over ~3h.",
      "evidence": "features.json SLICE-0.evidence + deploy/AC-evidence.txt"
    }
  ],
  "notes": [
    {
      "date": "2026-05-24",
      "kind": "lesson",
      "note": "Slice 0 caught 3 real bugs (nginx /api proxy, NaiveUI NInput wrapper, Node 25 Storage.clear missing) + 2 env quirks (SignOz port collision, Go 1.25 drift). See docs/lessons-learned-2026-05-24.md."
    },
    {
      "date": "2026-05-24",
      "kind": "handoff",
      "note": "Old repo huangbaixun/OpenAIOps archived 2026-05-24 (PR #8 merged, then gh repo archive). All non-doc code is in this new repo. Spec + ADR-0007 also live in this repo as docs/decisions/0001-initial-architecture.md (imported from old repo's docs/specs/2026-05-24-platform-rewrite-design.md)."
    }
  ],
  "known_drift": [
    {
      "date": "2026-05-24",
      "item": "AC-7 (goose down rollback) has SQL but no automated CI test. Slice 1 should add a CI step that runs goose down after goose up and re-asserts the schema diff.",
      "severity": "low",
      "tracked_in": "lessons-learned D3"
    },
    {
      "date": "2026-05-24",
      "item": "PGResolver does full table scan + bcrypt-verify all active keys per request. Acceptable at N<100 keys. Slice 1's ingester traffic will expose this. Add key_prefix CHAR(8) column for candidate narrowing.",
      "severity": "medium",
      "tracked_in": "lessons-learned D1"
    },
    {
      "date": "2026-05-24",
      "item": "migrate service in docker-compose installs goose via go install at every compose-up. Network-dependent + slow cold start. Switch to ghcr.io/pressly/goose image or pre-bake into Dockerfile.",
      "severity": "low",
      "tracked_in": "lessons-learned D2"
    }
  ],
  "open_questions": [
    {
      "date": "2026-05-24",
      "q": "Phase 2 sibling repo openaiops-ai — when to start? ADR-0007 said 'immediately after MVP'. But MVP = Slice 5 done, which is ~40 weeks of solo work away. Practical answer is probably 'after Slice 2 or 3' so AI agents have trace+log data to work with. Revisit at end of Slice 2.",
      "owner": "@huangbaixun"
    }
  ],
  "resolved_questions": [
    {
      "date": "2026-05-24",
      "q": "Old repo OpenAIOps fate?",
      "decision": "Archived via gh repo archive on 2026-05-24. Read-only. PR #8 merged the pivot ADR-0007 + spec + plan into old repo's main before archive."
    }
  ]
}
